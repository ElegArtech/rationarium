/**
 * @trame/contracts — contrat partagé client / serveur.
 *
 * Ce paquet est la définition unique dont dérivent la validation d'entrée du
 * serveur (via nestjs-zod), les types et les formulaires du client, et les
 * tests. Voir cadrage/03 § 4, D9.
 *
 * Contenu attendu, produit par le lot L-03 (vague 1) :
 *   - vocabularies.ts  — les vocabulaires de cadrage/01 § 4.1
 *   - permissions.ts   — le catalogue des ~125 permissions atomiques
 *   - roles.ts         — les 26 modèles de rôles
 *   - schemas/         — les schémas Zod par domaine
 *
 * Vague 0 : squelette seul, aucun code applicatif.
 */

export {};
