/* Remplacé par le lot L-40. Sortie 1 tant qu'il n'est pas écrit : un contrôle
   qui n'a rien à mesurer doit échouer, jamais réussir en silence. */
console.error("inoperant-check : pas encore écrit (L-40).");
process.exit(1);
